// const isNumber = (value) => !isNaN(inputValue[inputValue.length - 1]);

// const isMaxLength = (value, maxLen) => {
//   const valueToUse = value?.replace(/\s+/g, "");
//   if (valueToUse.length > maxLen) return true; //return value.slice(0, -1);
// };
